pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'Iniciando o Pipeline'
                 }
                 }
                 stage('Two') {
                 steps {
                    input('Deseja mesmo continuar?')
                 }
                 }
                 stage('Three') {
                 when {
                       not {
                            branch "master"
                       }
                 }
                 steps {
                       echo "não está na Brunch Master"
                 }
                 }
                 stage('Four') {
                 parallel { 
                            stage('Unit Test') {
                           steps {
                                echo "Running the unit test..."
                           }
                           }
                            stage('Integration test') {
                              agent {
                                    docker {
                                            reuseNode true
                                            image 'ubuntu'
                                           }
                                    }
                              steps {
                                echo "Running the integration test..."
                              }
                           }
                           }
                           }
              }
}
