pipeline {
    agent any
    
    stages {
               
        stage('Iniciar Container mario') {
            steps {
                sh '''
                   nohup docker run -i -p 8081:8080 pengbai/docker-supermario &
                   '''
            }
        }
        
        stage('espaço em disco') {
            steps {
                sh '''
                   sudo df -h
                   '''
            }
        }
    }
}
