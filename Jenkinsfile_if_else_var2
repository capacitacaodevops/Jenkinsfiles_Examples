pipeline{
    agent any
    stages{    
	stage("Conditions") {
		steps{
			script{
        		        sh 'echo "jenkins" > jenkins.txt'
				var="jenkins"
				if (var=="jenkins"){
                                       sh '''echo "Conteudo" > jenkins3.txt
					     var2=$(cat jenkins.txt) 
                                             echo $var2
                                          '''
                                       

					echo "if selecionado"
				}
				else { 
					echo "else selecionado"
        			}
    			}
		}
	}
     }
}
