pipeline {
    agent any
    options {
        timeout(time: 0.1, unit: 'SECONDS') 
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
