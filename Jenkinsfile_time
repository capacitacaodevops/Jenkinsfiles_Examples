pipeline {
    agent any
    options {
        timeout(time: 5, unit: 'SECONDS') 
    }
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
